{
  "name": "@engine/root",
  "version": "1.1.3",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "worker:test": "bun run --cwd ./apps/worker test",
    "worker:typecheck": "bun run --cwd ./apps/worker typecheck",
    "worker:cli": "bun run --cwd ./apps/worker cli",
    "api:typecheck": "bun run --cwd ./apps/api typecheck",
    "api:test": "bun run --cwd ./apps/api test"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "cli-table3": "^0.6.5",
    "commander": "^15.0.0",
    "hono": "^4.12.23",
    "js-yaml": "^4.1.1",
    "pdf-lib": "^1.17.1",
    "pino": "^10.3.1",
    "postgres": "^3.4.9",
    "vitest": "^4.1.8",
    "zod": "^4.4.3"
  }
}